// // 4. Есть значение. Добавить проверку на ввод только чисел. Если число, вывести true,
// // в противном случае false (isNaN)

var a = prompt('Введите значиние');

var inputValue = !isNaN(a);

console.log(inputValue)

